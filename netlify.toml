[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[dev]
  port = 8891
  targetPort = 5177
  envFiles = [".env"]

[functions."scheduled-gbp-expiry"]
  schedule = "@daily"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
